# ChromaDB API Service Dependencies
# This service runs on its own VM and handles all ChromaDB operations

# Core ChromaDB and ML dependencies
#chromadb==1.0.15
sentence-transformers==5.0.0
transformers==4.53.1
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<2.3.0
scikit-learn>=1.3.0,<1.7.0

# FastAPI and web server
fastapi>=0.100.0
uvicorn>=0.20.0
pydantic>=2.0.0,<3.0.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# NOTE: Install CPU-only PyTorch separately:
# pip install torch==2.7.1+cpu --index-url https://download.pytorch.org/whl/cpu
# Then install other dependencies: pip install -r requirements.txt

# NOTE: This service runs on a separate VM from the main app
# The main app only needs to make HTTP requests to this API