# RAG-Dev Application Dependencies (App VM Only)
# This file excludes ChromaDB and heavy ML dependencies
# ChromaDB operations are handled by the separate ChromaDB API service

# Google AI and API dependencies
google-generativeai==0.8.5
google-ai-generativelanguage==0.6.15
google-api-core==2.25.1
google-api-python-client==2.175.0
google-auth==2.40.3

# Web framework and server
Flask==3.1.1
streamlit==1.46.1
uvicorn==0.35.0

# File processing
python-pptx==1.0.2
openpyxl==3.1.5
pytesseract==0.3.13
Pillow==11.3.0

# Database and storage (PostgreSQL only)
supabase==2.18.1

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
pydantic>=2.0.0,<3.0.0
tqdm>=4.65.0
rich>=13.0.0

# Development and testing
pytest>=7.0.0

# NOTE: ChromaDB dependencies (chromadb, sentence-transformers, torch, etc.)
# are NOT included here. They run on the separate ChromaDB API service VM.
